<!DOCTYPE html>

<html>
    <head>
        <link rel = "stylesheet" href = "/css/style.css">
        <body>
            {{>header}}
            <h1>{{errorMessage}} page</h1>
            {{>footer}}
        </body>
    </head>
</html>     